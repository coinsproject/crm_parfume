# 🛡️ Как обновить приложение БЕЗ потери данных

## ⚠️ ВАЖНО: Всегда делайте резервную копию перед обновлением!

---

## 🌐 Работа с удаленным сервером

Если ваш сервер находится удаленно, сначала подключитесь к нему:

```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip
# или
ssh user@your-server-domain.com

# Перейдите в директорию проекта на сервере
cd /path/to/crm_parfume
```

**Если проект еще не скопирован на сервер:**

### Вариант 1: Клонирование из Git (РЕКОМЕНДУЕТСЯ)

```bash
# На сервере выполните:
cd /path/to/projects
git clone https://github.com/coinsproject/crm_parfume.git
cd crm_parfume

# Настройте docker-compose.yml и .env файлы
# Затем запустите приложение
docker-compose up -d
```

### Вариант 2: Копирование файлов через SCP

```bash
# С вашего локального компьютера скопируйте файлы на сервер
scp -r /local/path/to/crm_parfume user@your-server-ip:/remote/path/to/

# Или используйте rsync (более эффективно)
rsync -avz --exclude '*.db' --exclude '__pycache__' /local/path/to/crm_parfume/ user@your-server-ip:/remote/path/to/crm_parfume/
```

**Важно:** Не копируйте файлы базы данных (.db) - они должны создаваться на сервере!

---

## 🚀 Способ 1: Автоматический (РЕКОМЕНДУЕТСЯ)

Скрипт `update.sh` автоматически создаст резервную копию и обновит приложение.

### На удаленном сервере выполните:

```bash
# 1. Подключитесь к серверу по SSH
ssh user@your-server-ip

# 2. Перейдите в директорию проекта
cd /path/to/crm_parfume

# 3. Сделайте скрипт исполняемым (один раз)
chmod +x update.sh

# 4. Запустите обновление
./update.sh
```

**Если проект еще не на сервере:**
- Сначала склонируйте репозиторий: `git clone https://github.com/coinsproject/crm_parfume.git`
- Или скопируйте файлы через SCP/rsync (см. раздел выше)

**Что делает скрипт:**
1. ✅ Автоматически создаст резервную копию БД
2. ✅ Обновит код из Git
3. ✅ Применит миграции
4. ✅ Проверит работоспособность

---

## 📝 Способ 2: Ручное обновление (пошагово)

### Подготовка: Подключение к удаленному серверу

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Перейдите в директорию проекта
cd /path/to/crm_parfume
```

### Шаг 1: Резервная копия базы данных (ОБЯЗАТЕЛЬНО!)

```bash
# Создайте директорию для бэкапов (если её нет)
mkdir -p ./backups

# Создайте резервную копию БД
docker-compose exec crm sqlite3 /app/data/crm.db ".backup /app/data/crm_backup_$(date +%Y%m%d_%H%M%S).db"

# Или скопируйте файл напрямую
cp ./data/crm.db ./backups/crm_backup_$(date +%Y%m%d_%H%M%S).db

# Проверьте, что резервная копия создана
ls -lh ./backups/crm_backup_*.db
```

**Важно:** Убедитесь, что файл резервной копии существует и имеет нормальный размер!

### Шаг 2: Обновление кода

**Если проект уже на сервере и подключен к Git:**

```bash
# Получите последние изменения из Git
git fetch origin
git pull origin main

# Или обновитесь до конкретной версии v1.0.0
git checkout v1.0.0

# Проверьте, что изменения получены
git log --oneline -3
```

**Если проект не подключен к Git или нужно скопировать файлы:**

```bash
# Вариант 1: Клонировать заново (если нет важных локальных изменений)
cd /path/to
rm -rf crm_parfume  # ВНИМАНИЕ: Убедитесь, что БД сохранена!
git clone https://github.com/coinsproject/crm_parfume.git
cd crm_parfume
# Скопируйте обратно файл БД из резервной копии
cp /path/to/backup/crm.db ./data/crm.db

# Вариант 2: Обновить через rsync с локального компьютера
# На вашем локальном компьютере:
rsync -avz --exclude '*.db' --exclude '__pycache__' \
  /local/path/to/crm_parfume/ \
  user@your-server-ip:/remote/path/to/crm_parfume/
```

### Шаг 3: Пересборка и применение миграций

```bash
# Пересоберите Docker образ (если изменились зависимости)
docker-compose build

# Запустите контейнеры
docker-compose up -d

# Подождите 5 секунд, чтобы контейнер запустился
sleep 5

# Примените миграции базы данных (это безопасно, данные не удалятся)
docker-compose exec crm alembic upgrade head
```

### Шаг 4: Проверка работоспособности

```bash
# Проверьте, что контейнер запущен
docker-compose ps

# Проверьте health endpoint
curl http://localhost:8000/health

# Проверьте логи на ошибки
docker-compose logs --tail=50 crm
```

---

## 🔄 Откат (если что-то пошло не так)

Если после обновления что-то не работает, можно откатиться:

```bash
# 1. Остановите контейнер
docker-compose stop

# 2. Восстановите БД из резервной копии
cp ./backups/crm_backup_YYYYMMDD_HHMMSS.db ./data/crm.db

# 3. Откатите код к предыдущей версии
git log --oneline  # Найдите нужный коммит
git checkout <предыдущий-коммит>

# 4. Перезапустите
docker-compose restart
```

---

## ❓ Частые вопросы

### Q: Удалятся ли мои данные при обновлении?

**A: НЕТ!** Миграции Alembic только добавляют/изменяют структуру БД, но не удаляют данные. Однако **всегда делайте резервную копию** на всякий случай.

### Q: Что делать, если миграции не применяются?

```bash
# Проверьте текущую версию миграций
docker-compose exec crm alembic current

# Проверьте историю миграций
docker-compose exec crm alembic history

# Попробуйте применить вручную
docker-compose exec crm alembic upgrade head
```

### Q: Как часто нужно делать резервные копии?

Рекомендуется:
- Перед каждым обновлением
- Ежедневно (можно настроить автоматически через cron)

---

## 📋 Контрольный список перед обновлением

- [ ] Создана резервная копия БД
- [ ] Проверен размер резервной копии
- [ ] Проверена работоспособность текущей версии
- [ ] Прочитаны изменения в коммитах
- [ ] Готов к обновлению

---

## 📞 Полезные команды

```bash
# Просмотр логов в реальном времени
docker-compose logs -f crm

# Проверка статуса контейнеров
docker-compose ps

# Вход в контейнер (для отладки)
docker-compose exec crm /bin/bash

# Проверка размера БД
docker-compose exec crm sqlite3 /app/data/crm.db "SELECT page_count * page_size as size FROM pragma_page_count(), pragma_page_size();"
```

---

## 📚 Дополнительные инструкции

- **UPDATE_QUICK.md** - Краткая шпаргалка
- **DEPLOYMENT.md** - Подробная инструкция со всеми деталями

---

**Помните:** Резервная копия - это ваша страховка! Всегда делайте её перед обновлением! 🛡️

